@model TagViewModel

@{
    var vd = new ViewDataDictionary(ViewData.ModelMetadata, ViewData.ModelState);
    vd.Model = Model;

    vd["DisplayOnly"] = true;
}

@{ await Html.RenderPartialAsync("~/Views/Shared/EditorTemplates/TagViewModel.cshtml", vd); }
